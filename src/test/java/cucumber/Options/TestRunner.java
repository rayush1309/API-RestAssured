
package cucumber.Options;


//import io.cucumber.junit.Cucumber;
//import io.cucumber.junit.CucumberOptions;
import org.junit.runner.RunWith;
/*
@RunWith(Cucumber.class)
@CucumberOptions(
        features = "src/test/java/features", // Path to your feature files
        glue = {"step.definitions"} // Package where your step definitions reside
        // tags = "@SmokeTest", // Specify the tags for scenarios to execute
        // plugin = {"pretty", "html:target/cucumber-reports"} // Define the output format of test results
)
public class TestRunner {
    // This class doesn't need any code inside, as it's used for running Cucumber tests.
}
*/
